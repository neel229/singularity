package algo

const (
	threshold = 1000000
)

type data struct {
}

// Calculate takes in creator's data as input
// and outputs the initial value of a token
func Calculate() int64 {
	var worth int64

	return worth
}
